{
  "name": "obsidian-stats-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "harvest": "bun run src/main.ts",
    "harvest:pi": "scripts/harvest-pi.sh",
    "setup:pi": "scripts/install-user-service.sh",
    "logs:pi": "journalctl --user -u obsidian-stats-helper.service -n 100",
    "status:pi": "systemctl --user status obsidian-stats-helper.timer",
    "test": "bun test",
    "typecheck": "bun run tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "@types/bun": "1.3.13"
  }
}
